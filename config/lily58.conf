# Uncomment these to enable features. The lily58 shield's defconfig
# already enables split BLE, so nothing is required here for a basic build.

# CONFIG_ZMK_SLEEP=y
# CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=900000
